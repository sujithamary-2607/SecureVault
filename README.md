# SecureVault
SecureVault is a secure full-stack file sharing platform where users can register, log in, upload files, and share them with specific receivers. The system ensures authenticated access, enabling receivers to view and manage shared files in a personalized dashboard.
